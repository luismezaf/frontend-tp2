import React, { useState } from 'react';
import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    TextInput,
    Button
} from 'react-native';
export default () => {
    return(
        <View>
            <Text>
                Reservas!
            </Text>
        </View>
    );
}